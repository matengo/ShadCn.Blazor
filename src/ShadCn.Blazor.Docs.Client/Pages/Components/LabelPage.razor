@page "/components/label"

<PageTitle>Label Component - ShadCn.Blazor | Blazor UI</PageTitle>
<SeoHead 
    Description="Accessible form label component. Associates labels with form controls for better accessibility."
    Keywords="Label, Blazor, UI component, ShadCn.Blazor, form label, accessible, form control"
    Canonical="https://www.shadcn-blazor.dev/components/label"
    OgTitle="Label Component - ShadCn.Blazor | Blazor UI" />

<div class="space-y-8">
    <div class="space-y-4">
        <h1 class="text-3xl font-bold">Label</h1>
        <p class="text-lg text-muted-foreground">
            Renders an accessible label associated with controls.
        </p>
    </div>

    <Separator />

    <div class="space-y-8">
        <ExampleSection Title="Basic" Description="A basic label for form controls.">
            <ComponentExample Code="@BasicCode">
                <Preview>
                    <div class="w-full max-w-sm space-y-4">
                        <div class="space-y-2">
                            <Label For="email">Email</Label>
                            <Input id="email" type="email" Placeholder="Enter your email" />
                        </div>
                    </div>
                </Preview>
            </ComponentExample>
        </ExampleSection>

        <ExampleSection Title="With Checkbox" Description="Label with checkbox.">
            <ComponentExample Code="@WithCheckboxCode">
                <Preview>
                    <div class="flex items-center space-x-2">
                        <Checkbox Checked="@accepted" CheckedChanged="v => accepted = v" />
                        <Label>Accept terms and conditions</Label>
                    </div>
                </Preview>
            </ComponentExample>
        </ExampleSection>
    </div>
</div>

@code {
    private bool accepted = false;

    private const string BasicCode = @"<Label For=""email"">Email</Label>
<Input id=""email"" type=""email"" Placeholder=""Enter your email"" />";

    private const string WithCheckboxCode = @"<div class=""flex items-center space-x-2"">
    <Checkbox @bind-Checked=""accepted"" />
    <Label>Accept terms and conditions</Label>
</div>";
}
